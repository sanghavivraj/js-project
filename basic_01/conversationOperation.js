let score=null
console.log(typeof score)

let valueInNumber=Number(score)
console.log(typeof valueInNumber);
console.log(valueInNumber)

//"33"=>33
//"33abs" =>NaN
//true => 1
//false =>0;

let isLoggenIn=1;
let boolenIsLoggedIn=Boolean(isLoggenIn)
console.log(boolenIsLoggedIn);

// there are also string in which we can also use the type of to convert the any datatype to string.